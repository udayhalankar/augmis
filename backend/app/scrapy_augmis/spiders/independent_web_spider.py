# The active independent-web Scrapy spider is assembled inside
# `app.services.augmis_business_independent_discovery_service.run_scrapy_independent_scan`
# so it can call the shared AUGMIS page-processing pipeline directly.
